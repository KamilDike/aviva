import { StyleSheet } from "react-native";
import { Values } from "../../consts/Values";

export const WeatherScreenStyles = StyleSheet.create({
  cityName: {
    textAlign: "center",
    marginBottom: Values.L,
  },
});
