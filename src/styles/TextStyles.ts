import { StyleSheet } from "react-native";

export const TextStyles = StyleSheet.create({
  highlight: {
    fontWeight: "bold",
    fontSize: 18,
  },
  information: {
    textAlign: "center",
  },
});
